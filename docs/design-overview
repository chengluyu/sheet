## 编译器设计

编译器设计分为以下几个部分：

1. 词法分析器（lexer）的输入是字符流，输出是词法单元（token）流。
2. 语法分析器（parser）的输入是词法单元的流，输出是一个运行时描述对象。
3. 编译器（compiler）的输入是运行时描述对象，输出是字节码文件。
4. 虚拟机（virtual machine）的输入是字节码文件，负责执行字节码。

## 运行时描述对象

运行时描述对象用于描述虚拟机的运行时环境，

